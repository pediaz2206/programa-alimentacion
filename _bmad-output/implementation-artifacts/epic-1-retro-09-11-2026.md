---
epic: 1
date: 09-11-2026
verdict: pendiente
criteria: declared
headless: false
---

# Retrospectiva — Épica 1: Silvestre entra como entrenador y ve algo que es cierto

## Resumen de la épica

**Rango:** `99f5320^..8330595` · 8 commits · 19 archivos · +1391 −75

**Historias terminadas (6 de 6):**

| Historia | Commit |
|---|---|
| 1.1 Las cuentas sembradas tienen contraseña | `8330595` |
| 1.2 Las cuentas de prueba solo se vinculan entre ellas | `99f5320` |
| 1.3 Entrar con email y contraseña, solo donde está habilitado | `c7d09ed` |
| 1.4 El rol de entrenador existe como fila, y abre la pestaña | `e7bd078` |
| 1.5 La ficha del entrenador contesta si el trabajo rinde | `c90c51c` |
| 1.6 La pantalla dice qué es real y qué falta | `e8c3e20` |

La 1.1 se implementó **fuera de orden, durante esta retrospectiva**: la puerta de
Fase 1 detectó que el rango tenía cinco commits para seis historias y que
`sembrar.mjs` no generaba contraseñas. Estaba marcada `done` en
`sprint-status.yaml` sin estarlo. Se corrigió el estado, se implementó, y recién
entonces siguió la retro.

**Dónde vive el cambio:**

| Área | Volumen |
|---|---|
| `supabase/test/permisos.sql` | +194 |
| `supabase/schema.sql` | +177 −2 |
| `supabase/migraciones/` (006, 007) | +190 |
| `supabase/semillas/` | +186 −20 |
| `apps/web/src/` (9 archivos) | +403 −44 |
| `docs/`, `AGENTS.md`, `_bmad-output/` | +238 −4 |

Poco más de un tercio del cambio es verificación. No es casualidad: es donde la
épica podía verificarse.

## Evidencia disponible

| Insumo | Estado |
|---|---|
| Espec de la épica y criterios de aceptación | Disponible — `_bmad-output/planning-artifacts/epics.md`, criterios **declarados** |
| Diff y commits | Disponible — 8 commits, 0 merges, 0 revisiones binarias sin medir |
| Estado de sprint | Disponible — corregido durante Fase 1 |
| PRD y espina de arquitectura | Disponibles |
| Retrospectiva anterior | **No existe** — ésta es la primera |
| Archivos de historia por separado | **No existen** — las historias viven dentro de `epics.md` |
| Registros de sesión | **No disponibles** — las lecciones de proceso salen del propio historial de git y de la conversación, no de un registro estructurado |

## Hallazgos

*(en progreso — vistas agregadas derivadas; las lentes de revisión están corriendo)*

### Vista agregada: reconciliación espec ↔ implementación

**H-1 · AD-5 quedó desactualizado por el código.**
Fuente: `ARCHITECTURE-SPINE.md:101-109` contra `supabase/schema.sql:164`.
AD-5 dice, textual: *"`has_care_access(patient)` gobierna lectura y sigue valiendo
para cualquier rol vinculado y consentido"*, y *"Toda tabla clínica nueva declara
con cuál de las dos se lee y se escribe"*. La épica 1 agregó `ve_fotos()`, una
**tercera** puerta que angosta la lectura por rol. La decisión es correcta —la
pide FR-21— pero la espina dice dos puertas y el código tiene tres.
**Disposición:** reconciliar la espina.

**H-2 · Un criterio de aceptación de la 1.6 no se implementó y no se registró.**
Fuente: historia 1.6 en `epics.md` contra `apps/web/src/pantallas/Pacientes.tsx`.
El criterio dice: *"Given un paciente sin mediciones cargadas / Then el bloque
dice qué falta y cómo empezar, no muestra un cero"*. La ficha profesional **no
tiene ningún bloque de mediciones** —`profesional.ts:44` las trae, nadie las
pinta—. Durante la implementación sustituí ese caso por el del objetivo de
proteína, que es un hallazgo legítimo pero no es el criterio. La sustitución no se
le informó a nadie.
**Disposición:** decidir entre construir el bloque (entra en el alcance de la
épica 6) o corregir el criterio.

### Vista agregada: duplicación

**H-3 · `ve_fotos()` es `has_care_access()` más una línea.**
Fuente: `supabase/schema.sql:105` y `:164` — el diff entre los dos cuerpos es
exactamente `and r.rol = 'nutricionista'`.
Nada obliga a que sigan coherentes. Si la épica 2 parte `has_care_access` en ver y
prescribir y no toca `ve_fotos`, las dos van a decir cosas distintas sobre el mismo
vínculo. Ya está anotado en la validación de `epics.md`; acá queda medido.
**Disposición:** deuda conocida, se cierra en la épica 2.

**H-4 · La autoridad del aislamiento vive en dos lugares.**
Fuente: `supabase/migraciones/006-aislar-cuentas-de-prueba.sql`.
Las políticas de `insert` y `update` gobiernan dos de los tres caminos hacia un
vínculo; el tercero —`reclamar_invitaciones()`— es `SECURITY DEFINER` y no pasa por
ninguna política, así que la misma condición está escrita adentro de la función.
Es exactamente lo que AD-2 advierte (*"parte la autoridad en dos lugares"*),
forzado por cómo funciona `SECURITY DEFINER`. Mitigado con una aserción por
camino.
**Disposición:** aceptado, registrado para que no se re-descubra.

### Vista agregada: crecimiento de archivos

**H-5 · `Pacientes.tsx` es el archivo más grande de la app y va a seguir creciendo.**
Fuente: 478 → 569 líneas en la épica; 11 componentes en un archivo. La siguiente
pantalla más grande es `Hoy.tsx` con 355.
Las épicas 6 y 7 le suman las dos fichas completas y el botón de propuesta, y las
tres tocan este mismo archivo —ya anotado como riesgo de churn en la validación—.
**Disposición:** partirlo antes de la épica 6, no después.

### Vista agregada: delta de arquitectura

Sin violaciones de capa ni ciclos nuevos. Las funciones SQL nuevas son ayudantes de
**lectura**, no escrituras de una sola tabla, así que no chocan con la prohibición
de AD-2. `registro_sin_detalle` declara `security_invoker = true`, que es
obligatorio en este esquema, y hay una aserción que falla si se lo sacan.

### Lentes de revisión de código

*(pendiente — adversarial, casos borde y huecos de verificación corriendo)*

## Verificación de comportamiento

**Lo ejercitado:**

- 54 aserciones de RLS sobre un Postgres 16 efímero, todas pasan. Cada guarda
  nueva se verificó **quitándola** y confirmando que su aserción falla:
  la condición de rol en `ve_fotos`, `security_invoker` en la vista, y las tres
  guardas de aislamiento de la 1.2.
- 241 tests de `packages/core`, todos pasan.
- Bundle de producción construido en los dos sentidos: sin la bandera, el texto
  del formulario no aparece en `dist/`; con la bandera, sí.
- Pantallas renderizadas en Chromium a 320 y 390 px, claro y oscuro. Contraste
  medido: 5.23:1 y 7.23:1.
- `entregar()` y `contrasenaAlAzar()` de la semilla: 500 contraseñas distintas de
  500; los cuatro caminos de entrega; permisos 600 del archivo.

**Lo NO ejercitado, y es el hueco más grande de la épica:**

Nada corrió contra un Supabase real. Nadie inició sesión con el formulario, nadie
abrió la pestaña de entrenador con una sesión de verdad, nadie pidió una foto
siendo entrenador contra el storage real. Las políticas están probadas contra
Postgres desnudo con un arnés que simula `auth.uid()`; la app está probada contra
HTML estático. **El camino entero, punta a punta, no se ejecutó nunca.**

## Ítems de acción

*(pendiente — Fase 4)*

## Veredicto de aceptación

*(pendiente — Fase 4)*

## Preguntas abiertas

*(pendiente)*
