[build]
  base = "."
  command = "npm run build --workspace @pa/web"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"

# La PWA es una SPA: todas las rutas sirven el mismo index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
